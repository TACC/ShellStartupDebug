Shell Startup Debug 2.0+
   (2.0) * Use git archive to produce tar ball
         * Fix configure.ac and Bash.lua to not depend on auto string to number conversions
	 * Require luaposix outside of strict
	 
